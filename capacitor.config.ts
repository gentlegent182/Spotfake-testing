import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Spotifake testing',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
